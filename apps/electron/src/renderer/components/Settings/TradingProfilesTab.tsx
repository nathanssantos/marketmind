import { TradingProfilesManager } from '@renderer/components/Trading/TradingProfilesManager';
import type { ReactElement } from 'react';

export const TradingProfilesTab = (): ReactElement => {
  return <TradingProfilesManager />;
};
