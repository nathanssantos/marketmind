export const LIQUIDITY_HEATMAP = {
  BUCKET_DURATION_MS: 60_000,
  MAX_BUCKETS_MEMORY: 500,
  SAMPLE_INTERVAL_MS: 2_000,
  PERSIST_BATCH_SIZE: 10,
  MAX_DB_AGE_MS: 7 * 24 * 60 * 60 * 1000,
  SNAPSHOT_MAX_BUCKETS: 200,
} as const;
