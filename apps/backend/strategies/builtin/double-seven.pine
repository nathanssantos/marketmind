// @id double-seven
// @name Double Seven
// @version 1.0.0
// @description Larry Connors' Double Seven strategy - buy at 7-day low close, sell at 7-day high close. From 'Short-Term Trading Strategies That Work'. Research shows consistent 80%+ win rate since 1995.
// @author MarketMind
// @tags mean-reversion,connors,classic,proven,swing
// @strategyType MEAN_REVERSION
// @momentumType REVERSAL
// @param lookbackPeriod Days for high/low detection
// @param smaTrend Trend filter SMA period
// @param atrPeriod ATR for stop loss
// @param slMultiplier Stop loss ATR multiplier

//@version=5
indicator('Double Seven', overlay=true)

lookbackPeriod = input.int(7, 'lookbackPeriod', minval=5, maxval=10, step=1)
smaTrend = input.int(200, 'smaTrend', minval=100, maxval=250, step=50)
atrPeriod = input.int(14, 'atrPeriod', minval=10, maxval=20, step=2)
slMultiplier = input.float(2.5, 'slMultiplier', minval=1.5, maxval=4, step=0.5)

nDayHighLow_isNDayHigh = high >= ta.highest(high, lookbackPeriod)
nDayHighLow_isNDayLow = low <= ta.lowest(low, lookbackPeriod)
sma200 = ta.sma(close, smaTrend)
atr = ta.atr(atrPeriod)
atrVal = atr

volSma20 = ta.sma(volume, 20)

longEntry = (nDayHighLow_isNDayLow == 1 and close > sma200)
shortEntry = (nDayHighLow_isNDayHigh == 1 and close < sma200)

confBase = 70
confBonus0 = close > sma200 ? 10 : 0
confBonus1 = volume > ta.sma(volume, 20) ? 5 : 0
confValue = math.min(confBase + confBonus0 + confBonus1, 90)

exitLong = false
exitShort = false

sig = longEntry ? 1 : shortEntry ? -1 : 0
sl = longEntry ? close - atrVal * 0.6 : shortEntry ? close + atrVal * 0.6 : na
tp = longEntry ? close + math.abs(close - (close - atrVal * 0.6)) * 2 : shortEntry ? close - math.abs((close + atrVal * 0.6) - close) * 2 : na
conf = longEntry or shortEntry ? confValue : 0
exitSig = exitLong ? 1 : exitShort ? -1 : 0

plot(sig, 'signal', display=display.none)
plot(sl, 'stopLoss', display=display.none)
plot(tp, 'takeProfit', display=display.none)
plot(conf, 'confidence', display=display.none)
plot(exitSig, 'exitSignal', display=display.none)
