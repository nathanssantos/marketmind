// @id bear-trap
// @name Bear Trap
// @version 1.0.0
// @description False breakdown below support followed by rapid recovery. Entry LONG when price breaks below support then closes back above with volume confirmation.
// @author Classic Technical Analysis
// @tags trap,reversal,false-breakdown,counter-trend
// @strategyType MEAN_REVERSION
// @momentumType REVERSAL
// @param lookbackPeriod Period to find support levels
// @param emaPeriod EMA period for trend confirmation
// @param volumeMultiplier Volume multiplier for confirmation
// @param atrPeriod ATR period for stop calculation

//@version=5
indicator('Bear Trap', overlay=true)

lookbackPeriod = input.int(20, 'lookbackPeriod', minval=10, maxval=50, step=5)
emaPeriod = input.int(20, 'emaPeriod', minval=10, maxval=50, step=5)
volumeMultiplier = input.float(1.3, 'volumeMultiplier', minval=1, maxval=2, step=0.1)
atrPeriod = input.int(14, 'atrPeriod', minval=7, maxval=21, step=1)

ema = ta.ema(close, emaPeriod)
atr = ta.atr(atrPeriod)
atrVal = atr

volSma20 = ta.sma(volume, 20)

longEntry = (low[1] < low[2] and close > low[2] and close > ema)
shortEntry = false

confBase = 60
confBonus0 = volume > ta.sma(volume, 20) ? 15 : 0
confBonus1 = close > ema ? 10 : 0
confValue = math.min(confBase + confBonus0 + confBonus1, 95)

exitLong = false
exitShort = false

sig = longEntry ? 1 : shortEntry ? -1 : 0
sl = longEntry ? close - atrVal * 0.6 : shortEntry ? close + atrVal * 0.6 : na
tp = longEntry ? close + math.abs(close - (close - atrVal * 0.6)) * 2.5 : shortEntry ? close - math.abs((close + atrVal * 0.6) - close) * 2.5 : na
conf = longEntry or shortEntry ? confValue : 0
exitSig = exitLong ? 1 : exitShort ? -1 : 0

plot(sig, 'signal', display=display.none)
plot(sl, 'stopLoss', display=display.none)
plot(tp, 'takeProfit', display=display.none)
plot(conf, 'confidence', display=display.none)
plot(exitSig, 'exitSignal', display=display.none)
