// @id morning-star
// @name Morning Star
// @version 1.0.0
// @description Morning Star e Evening Star patterns. Padrão de 3 candles de reversão forte. Star indica indecisão após tendência.
// @author MarketMind
// @tags reversal,candlestick,morning-star,pattern
// @strategyType ANY
// @momentumType REVERSAL
// @param atrMultiplier ATR para stop

//@version=5
indicator('Morning Star', overlay=true)

atrMultiplier = input.float(2, 'atrMultiplier', minval=1.5, maxval=3, step=0.5)

atr = ta.atr(14)
atrVal = atr

volSma20 = ta.sma(volume, 20)

longEntry = (close[2] < open[2] and (math.abs(close[1] - open[1])) < (math.abs(close[2] - open[2]) * 0.3) and close > open and close > ((open[2] + close[2]) / 2))
shortEntry = (close[2] > open[2] and (math.abs(close[1] - open[1])) < (math.abs(close[2] - open[2]) * 0.3) and close < open and close < ((open[2] + close[2]) / 2))

confBase = 75
confBonus0 = volume > ta.sma(volume, 20) ? 10 : 0
confValue = math.min(confBase + confBonus0, 90)

exitLong = false
exitShort = false

sig = longEntry ? 1 : shortEntry ? -1 : 0
sl = longEntry ? close - atrVal * 0.6 : shortEntry ? close + atrVal * 0.6 : na
tp = longEntry ? close + math.abs(close - (close - atrVal * 0.6)) * 2.5 : shortEntry ? close - math.abs((close + atrVal * 0.6) - close) * 2.5 : na
conf = longEntry or shortEntry ? confValue : 0
exitSig = exitLong ? 1 : exitShort ? -1 : 0

plot(sig, 'signal', display=display.none)
plot(sl, 'stopLoss', display=display.none)
plot(tp, 'takeProfit', display=display.none)
plot(conf, 'confidence', display=display.none)
plot(exitSig, 'exitSignal', display=display.none)
